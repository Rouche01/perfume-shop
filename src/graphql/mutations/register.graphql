mutation Register($input: customRegisterInput) {
  customRegister(input: $input) {
    userData {
      jwt
      user {
        id
        username
        confirmed
        createdAt
      }
    }
    error {
      message
    }
  }
}