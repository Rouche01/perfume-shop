mutation Login($input: String) {
  customLogin (input: {token: $input}) {
    userData {
      jwt
      user {
        id
        username
        confirmed
        createdAt
      }
    }
    error {
      message
    }
  }
}