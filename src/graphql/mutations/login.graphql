mutation Login($firebaseToken: String) {
  customLogin (input: {token: $firebaseToken}) {
    userData {
      jwt
      user {
        id
        username
        confirmed
        createdAt
      }
    }
    error {
      message
    }
  }
}